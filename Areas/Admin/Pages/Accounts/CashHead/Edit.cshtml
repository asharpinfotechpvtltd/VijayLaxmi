@page
@model VijayLaxmi.Areas.Admin.Pages.Accounts.CashHead.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CashHeads</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CashHeads.Id" />
            <div class="form-group">
                <label asp-for="CashHeads.CashHead" class="control-label"></label>
                <input asp-for="CashHeads.CashHead" class="form-control" />
                <span asp-validation-for="CashHeads.CashHead" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
